---
phase: 5
plan: 7
wave: 1
---

# Plan 5.7: Long-Range Historical Ingestion Upgrade

## Objective
Upgrade the Binance ingestion system to handle massive data fetches (2017 to present), adding robust retry logic, OHLC validation, detailed progress tracking, and configurable parameters.

## Context
- backend/market_data/ingestion/fetch_binance.py
- backend/config.py

## Tasks

<task type="auto" effort="medium">
  <name>Add Ingestion Configuration</name>
  <action>
    Update `backend/config.py` with environment variables for `INGEST_START_DATE`, `INGEST_SYMBOLS`, `INGEST_TIMEFRAMES`, and `INGEST_BATCH_SIZE`.
  </action>
  <verify>grep "INGEST_START_DATE" backend/config.py</verify>
  <done>Config variables exposed successfully.</done>
</task>

<task type="auto" effort="high">
  <name>Upgrade Fetch Logic</name>
  <action>
    Update `backend/market_data/ingestion/fetch_binance.py`:
    - Add retry loop with exponential backoff to handle rate limits.
    - Implement OHLC consistency checks (high >= low, etc.) to reject malformed records safely.
    - Format progress logs as `[SYMBOL][TIMEFRAME] Fetching candles: START -> END`.
    - Loop through configuration dates instead of hardcoded 7-day ranges.
  </action>
  <verify>grep "high >=" backend/market_data/ingestion/fetch_binance.py</verify>
  <done>Long-range fetch architecture is robust and validated.</done>
</task>

## Success Criteria
- [ ] Process can flawlessly fetch 7 years of 1h BTC data.
- [ ] Safe database inserts using conflict resolution.
- [ ] Bad data (impossible OHLC) is caught and skipped.
