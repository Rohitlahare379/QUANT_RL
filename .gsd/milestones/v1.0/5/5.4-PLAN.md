---
phase: 5
plan: 4
wave: 1
---

# Plan 5.4: Market Data Normalization Layer

## Objective
Abstract exchange-specific data structures (like Binance Klines) into a unified internal representation to support multi-exchange ingestion seamlessly.

## Context
- backend/market_data/ingestion/fetch_binance.py
- backend/market_data/storage/models.py

## Tasks

<task type="auto" effort="medium">
  <name>Create Normalization Schemas</name>
  <action>
    Create `backend/market_data/normalizers/schemas.py`.
    Define a Pydantic model `NormalizedCandle` that validates symbol, timeframe, timestamp, open, high, low, close, volume, and source.
  </action>
  <verify>grep "NormalizedCandle" backend/market_data/normalizers/schemas.py</verify>
  <done>Schema defined and validates correct types.</done>
</task>

<task type="auto" effort="medium">
  <name>Build Base and Binance Normalizers</name>
  <action>
    Create `backend/market_data/normalizers/base.py` with an abstract base class.
    Create `backend/market_data/normalizers/binance.py` containing the logic to parse raw Binance arrays into `NormalizedCandle` lists.
  </action>
  <verify>grep "BinanceNormalizer" backend/market_data/normalizers/binance.py</verify>
  <done>Normalizers successfully extract logic from ingestion scripts.</done>
</task>

<task type="auto" effort="low">
  <name>Refactor Ingestion Service</name>
  <action>
    Modify `backend/market_data/ingestion/fetch_binance.py` to import and use `BinanceNormalizer` instead of doing internal pandas conversions.
  </action>
  <verify>grep "BinanceNormalizer" backend/market_data/ingestion/fetch_binance.py</verify>
  <done>fetch_binance.py refactored to use standard normalization layer.</done>
</task>

## Success Criteria
- [ ] Centralized normalization layer ensures dirty data never reaches storage.
- [ ] Binance arrays are successfully converted into standard dictionaries.
- [ ] Easily extensible for Alpaca or CSV data sources.
